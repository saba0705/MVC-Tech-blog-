<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="styles.css">
    <title>Your Dashboard</title>
    
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <h2 class="text-center">Your Dashboard</h2>
            </div>
        </div>

        <div class="row">
            <div class="col-12 blog-container" id="blog-container">
               {{#each blogs}}
                <div class="card">
                    <div class="card-header">{{title}}</div>
                    <div class="card-body">{{content}}</div>
                    <div class="card-body">{{format_date date_created}}</div>
              <a href="/edit/{{id}}"   > <button class="btn btn-primary update w-25">Update</button></a>  
                    <button data-id={{id}} class="btn btn-danger delete w-25">Delete</button>
                </div>
   

               {{/each}}
            </div>
        </div>
    </div>

    <div class="overlay" id="overlay"></div>

    <div class="container-fluid d-flex flex-column justify-content-end full-height">
        <div class="row justify-content-center mb-3">
            <div class="col-md-6 d-flex justify-content-center position-relative">
                <button class="btn btn-primary custom-button position-absolute" id="new-blog" style="bottom: 0;">+ New Blog</button>
            </div>
        </div>
    </div>

    <div class="container">
        <form class="form new-blog-form" id="blog-form">
            <div class="form-group">
                <label for="blog-name">Blog Title:</label>
                <input class="form-input" type="text" id="blog-name" name="blog-name" required />
            </div>

            <div class="form-group">
                <label for="blog-desc">Description:</label>
                <textarea class="form-input" id="blog-desc" name="blog-desc" required></textarea>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary">Save</button>
            </div>
        </form>
    </div>
</body>

<script src = "/js/dashboard.js"></script>

</html>